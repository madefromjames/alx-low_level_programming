#include <stdlib.h>
#include "main.h"

/**
 * _abs - absolute value of an integer.
 * @: absolute integer
 *
 * Return: Always 0
 */
int _abs(int r)
{
	if (r < 0)
		_putchar(r);
	else
		_putchar(r);
	return (0);
}

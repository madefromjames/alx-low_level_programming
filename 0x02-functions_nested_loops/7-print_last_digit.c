#include "main.h"

/**
 * print_last_digit - print last digit
 * @i: return last digit
 *
 * Return: 0;
 */
int print_last_digit(int i)
{
	int last = 2;

	if (i % last)
		return (last);

	return (0);
}

#include "main.h"

/**
 * print_last_digit - Prints the last digit of a number
 * @v: check the digit
 *
 * Return: Always 0
 */

int print_last_digit(int v)
{
	if (v % 25)
		_putchar(v);
	return (0);
}

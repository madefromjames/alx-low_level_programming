#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
//	int i;

	printf("Infinite loop incoming :\(");

/*	i = 0;

	while (i < 10)
	{
		putchar(i);
	}*/
	putchar(10);
	printf("Infinite loop avoided! \\o/");

	putchar(10);
	return (0);
}

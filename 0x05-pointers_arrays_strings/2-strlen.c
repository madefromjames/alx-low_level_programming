#include "main.h"
#include <string.h>

/**
 * _strlen - length of string
 * @s: string parameter
 * Return: length
 */
int  _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}

	return (len);
}
